# ByteTrack config for Ultralytics model.track(). Tuned for basketball: keep
# lost tracks alive through brief occlusion so IDs survive contact/screens.
tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 60
match_thresh: 0.85
fuse_score: true
